Check your environoment variables
if PYTHONPATH exists:
	Put these files inside the folder that is referenced in the PYTHONPATH variable

if PYTHONPATH doesnt exist:
	Create a variable called PYTHONPATH and paste the path to a folder
	Places these files in that folder